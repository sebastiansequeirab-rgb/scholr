# Handoff 2026-05-08 · Scholar / Skolar

Repo `scholr` · canon prod `https://scholr-5x9n.vercel.app/` (proyecto Vercel `scholr`).
Branch `main`. Working dir: `/Users/sebastiansequeira/Library/Mobile Documents/com~apple~CloudDocs/Scholar/skolar/`.

Este doc resume **dónde quedó todo** después del rebuild de `/notes` y qué pantallas/tareas vienen.
Para arrancar un chat nuevo, leelo primero junto con `CLAUDE.md` y `active/execution/REDESIGN_HANDOFF.md`.

---

## ✅ Pantallas v2 ya pixel-perfect contra mock

| Path | Commits clave | Estado |
|---|---|---|
| `/dashboard` | `089ecec` | DashMetaBar + UrgentCountdown + hero + agenda + próximas. Real data. |
| `/calendar` | `beef3ff`, `d1f2739` | Custom month/week/day. FullCalendar fuera. |
| `/subjects` | (v2 base) | Cards + detail modal. Validar 1:1 vs mock pendiente. |
| `/ai` | `8b34f73` → `4c0830f` | Chat tipo ChatGPT con `ai_sessions` reales + tools full-data. |
| `/tareas` | `9400316`, `8b856ec`, `686eb5f`, `76bf615`, `adf8059`, `4bc1e61` | Kanban DnD + Supabase real + drawer detalle + `?new=1`. |
| `/evaluaciones` | mismos | Timeline + drawer + study plan editable + `ExamCreateForm` + `?new=1`. |
| **`/notes`** | **`151e056`, `7c168cb`** | **Rebuild visual v2 esta sesión** — ver §"Estado /notes" abajo. |

---

## Estado /notes (recién rebuildeado, 2026-05-08)

### Commits

- **`151e056`** `feat(notes): rebuild visual v2 — 3-pane editorial, dual-style title, expanded toolbar`
- **`7c168cb`** `fix(notes): cuadrar shell — full-bleed bajo topbar, toolbar 1 fila, paddings tighter`

### Lo que cambió

1. **Header de página viejo eliminado** (`CUADERNO · N · apuntes`). El título vive en el editor.
2. **Shell `position: fixed` (≥1024px)** anclado a `top:52px / left:60px / right:0 / bottom:0` — escapa el `max-w-[1240px]` y el padding `lg:pt-5 lg:px-6 lg:pb-10` del layout. `document.body.style.overflow = 'hidden'` mientras la página está montada.
3. **Tres columnas: 240px / 340px / 1fr** (folders / list / editor).
4. **Folders v2** (`.notes-folders-v2`, `.folder-v2`):
   - Solo materias con notas (`countBySubject(s.id) > 0`) + carpeta "Sin materia" como fallback al final si hay huérfanas.
   - Eyebrow `CARPETAS` + botón "+" 16px → `<Link href="/subjects">`.
   - Activa: `bg --accent-soft`, dot brighter (box-shadow), nombre bold.
5. **Notes list v2** (`.notes-list-v2`, `.note-card-v2`):
   - Eyebrow dinámico `${subjectAbbr(name)} · ${name.toUpperCase()}` (helper `subjectAbbr` toma siglas de palabras o primeras 4 letras).
   - Título `{N} apuntes` sans-bold 18px + botón `.btn-new` "+ Nueva".
   - Segmented `.seg/.seg__btn` Recientes/A-Z reusado.
   - Card: padding 16, radius lg. Activa: `border-left 3px var(--success)` + bg `color-mix(success 6%)`.
   - Fechas vía `relativeNoteDate(date, lang)` en `lib/utils.ts`.
6. **Editor v2** (`.notes-editor-v2`):
   - Eyebrow contextual `{ABBR} · {COUNT} APUNTES · ÚLTIMA EDICIÓN {when}` mono UPPERCASE 11px. Mientras `saveStatus === 'saving'` muestra "GUARDANDO…". Re-render cada 60s con `setInterval`.
   - Acciones derecha: `ios_share` (no-op) + `more_horiz` (popover con "Eliminar nota").
   - **Título dual-style** (`TitleField` component): split en em-dash ` — `. Cuando blureado renderiza `<span class="title-prefix">{prefix}</span><span class="title-dash"> — </span><span class="title-suffix">{suffix}</span>` (suffix = `var(--font-serif) italic var(--info)`). Click → input plano.
7. **Toolbar redesign** (`.note-toolbar-v2`): single-row `flex-wrap: nowrap; overflow-x: auto`.
   - Dropdown `HeadingDropdown` (Texto normal / H1 / H2 / H3).
   - Grupo 1: bold · italic · underline · strike · highlight.
   - Grupo 2: bullet · numbered · indent (`sinkListItem('listItem')`) · checklist.
   - Grupo 3: **fx** (Mathematics → `editor.chain().insertInlineMath({ latex })`) · code · blockquote · table 3×3.
   - Grupo 4: attach (no-op disabled) · image · mic · ✦ AI sparkles.
8. **AI sparkles** (`AIPopover`): popover con input + 2 chips ("Resumir nota", "Mejorar redacción") → `POST /api/ai` con `message = ${instruction}\n\n---\nNota actual:\n${plainText}`, `app_context = { current_page: 'notes', active_subject_id, language, subject_count }`. Reply se inserta con `editor.chain().focus().insertContent('\n\n' + reply).run()`.
9. **Empty state** SIEMPRE en español: "Elige una nota / Selecciónala en la lista o crea una nueva. / + Nueva nota".
10. **Auto-save 1s → 2s** (debounce raised). Auto-title sigue funcionando (extrae H1/H2/H3 → primer plain text → fallback fecha).

### Deps nuevas instaladas

- `@tiptap/extension-underline` `^3.22.5`
- `@tiptap/extension-highlight` `^3.22.5`
- `@tiptap/extension-table` (re-exporta TableRow/TableCell/TableHeader)
- `@tiptap/extension-mathematics` `^3.22.5`
- `katex` `^0.16.45`
- `katex/dist/katex.min.css` importado en `app/globals.css` (línea 2).

### i18n notes block extendido (es+en parity verificada)

Nuevas keys: `folders`, `newFolder`, `countLabel`, `countLabelOne`, `newShort`, `editor.lastEdited/savingShort/share/more/delete`, `toolbar.normal/h1/h2/h3/bold/italic/underline/strike/highlight/bullet/numbered/indent/checklist/formula/code/quote/table/attach/image/voice/ai`, `empty.title/sub/cta`, `ai.placeholder/suggest_summary/suggest_improve/loading/error`.

### Helper agregado

`relativeNoteDate(input: string | Date, lang: 'es' | 'en')` en `lib/utils.ts:148`:
- mismo día → `Hoy · HH:mm` / `Today · HH:mm`
- ayer → `Ayer` / `Yesterday`
- 2–6 días → `<DOW> · HH:mm` (`Mié · 19:30`)
- 7–13 días → `Sem. pasada` / `Last week`
- ≥14 días → `8 abr` / `Apr 8`

### Bloque CSS (~390 líneas) en `app/globals.css`

Header `═══ NOTES v2 · 3-pane editorial shell ═══`. Clases con sufijo `-v2` para no chocar con el viejo `.notes-shell`/`.note-item`/`.notes-toolbar*` (esos quedan; podríamos limpiarlos en una pasada futura).

---

## 🔄 Pendientes inmediatos (lo que más probablemente venga)

### 1. `/calificaciones` — pantalla NUEVA (deferida en la sesión de notes)

El usuario pasó spec completo de Claude design en la misma sesión donde rebuildeamos /notes, pero acordamos hacerla en turno aparte. La spec está en el chat anterior (no en archivo). Resumen:

**Ruta nueva**: `/calificaciones` con item en sidebar (ícono gráfico, entre Evaluaciones y Apuntes).
**Layout**: vertical scroll: header → 4 KPIs → tabla por materia → gráfico evolución → últimas evaluaciones.

Highlights:
- Header con eyebrow `CALIFICACIONES · CICLO 2026 · 1ER SEMESTRE` + título "Tu *promedio* general" (italic info en "promedio") + sub "13.6 / 20 · 6 materias · 18 evaluaciones registradas · 4 pendientes" + botones Filtrar ciclo / Exportar.
- 4 KPI cards: Promedio General (48px serif italic + sufijo /20 + delta success), Materias Aprobando (5/6 + warning "1 en riesgo"), Mejor Materia (Programación Av. · prom 17.8), En Riesgo (Trading Algorítmico · prom 9.2 danger).
- Tabla detalle: MATERIA · PROMEDIO ACTUAL · PROYECCIÓN · PARA APROBAR · ESTADO (badge SkolarGrades) · PROGRESO (barra % evaluado) · chevron.
- Gráfico SVG simple (no Recharts): líneas con eje X meses ene-jul, eje Y 0-20, marca dashed 9.5 warning + línea info. Puntos color materia con tooltip.
- Lista últimas evaluaciones: estilo /evaluaciones compacto, border-left 3px materia + chip materia + tipo + peso + título + nota mono 22px (success ≥14 / warning 9.5–13.9 / danger <9.5).

**Hard rules** (del usuario):
- No tocar arquitectura ni API de SkolarGrades / SUBJECTS_DATA.
- Usar `lib/grades.ts` (`statusBg`, `statusFg`, `summarize`, `classify`, `projectIfAvg`) — NO duplicar lógica.
- Tokens semánticos exactos · no hex.
- Click fila tabla → navega detalle de materia.
- Click evaluación → drawer detalle (placeholder console.log inicial).

### 2. Posibles iteraciones a /notes

- **Attachments rail al pie del editor** (mock lo muestra: `Cap_4_Anualidades.pdf` + `Audio clase 22 abr`). Falta tabla `notes_attachments` o columna jsonb en `notes`. Si el usuario lo pide → migración + UI.
- **"+" CARPETAS como creación inline de carpeta** (hoy navega a /subjects). Spec lo pide implícito; si lo enfatiza → modal/inline de creación de Subject ligero.
- **Acciones share/more reales**. Hoy `share` es no-op y `more` solo borra.
- **Feature: foco/zen mode** del editor (ocultar las 2 cols y dejar solo el editor full-width).

### 3. Limpieza opcional

- CSS viejo `.notes-shell`, `.notes-sidebar`, `.notes-list`, `.notes-editor`, `.note-item`, `.notes-toolbar*`, `.folder` (no `.folder-v2`) en `app/globals.css` líneas 2057–2199 ya no se usa. Borrar para reducir ~140 líneas.
- Untracked en repo (no son del rebuild — vienen de antes): `active/execution/SPRINT{1..4}.md`, `active/execution/PROMPTS.md`, `active/skills/`, `app/fonts/skolar-{dark,light}-logo.png`. Decidir si commitear o `.gitignore`.

### 4. Otras pantallas (de project_scholar_redesign_v2)

- `/subjects` — validar 1:1 vs mock (sin trabajo pendiente reportado, pero el usuario podría pedir pasada de pulido).
- `/settings` — `.settings-action` y `.settings-field__name` serif italic pendiente revisión 1:1.
- `/planner` — pantalla vieja, intacta. Ningún link interno apunta acá. Decidir si borrar la ruta.

---

## Workflow probado para cada pantalla

1. Leé este handoff + `CLAUDE.md` + `REDESIGN_HANDOFF.md`.
2. Si el usuario pasa **prompt + 2 imágenes (estado actual + mock)**, entrá en plan mode y proponé plan numerado por sección antes de tocar código.
3. CSS al final de `app/globals.css` con header `═══ <PANTALLA> · "<title>" ═══`. Sufijo `-v2` para clases que conviven con la versión vieja.
4. Componentes client a `features/<area>/components/` o inline si son chicos.
5. i18n en `i18n/es.json` + `i18n/en.json` en sync (parity check con el script de abajo).
6. Verificación: `npx tsc --noEmit` (debe quedar limpio) + i18n parity + `npm run dev` smoke.
7. Commit narrativo en español por sección + `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`.
8. `git push origin main` → Vercel auto-deploya `scholr` (canon).

### i18n parity script

```bash
cd skolar && node -e "
const es=require('./i18n/es.json'),en=require('./i18n/en.json');
function k(o,p=''){let r=[];for(const x of Object.keys(o)){const v=o[x];if(v&&typeof v==='object'&&!Array.isArray(v))r=r.concat(k(v,p+x+'.'));else r.push(p+x);}return r;}
const a=new Set(k(es)),b=new Set(k(en));
console.log('es-only:',[...a].filter(x=>!b.has(x)));
console.log('en-only:',[...b].filter(x=>!a.has(x)));
"
```

Esperado: ambos `[]`.

---

## Hard constraints (recordatorio)

- ❌ No mover arquitectura (rutas, FullCalendar, SubjectDetail modal, providers)
- ❌ No cambiar `lib/grades.ts` API
- ❌ No cambiar schema Supabase ni RLS
- ❌ No hex hardcoded — siempre tokens (`--success/--warning/--danger/--info`, `--color-primary/-container`, `--color-tertiary/-container`, etc.)
- ❌ No `style={{ ... }}` con hex literal — pasar `--accent-color` con `style={{ ['--accent-color' as never]: subject.color }}`
- ❌ No tocar middleware ni rutas auth ni `features/ai/provider.ts`
- ❌ No usar `any` en TypeScript (regla del repo)
- ✅ Helpers: `subjectTag(color)`, `subjectInfo(id, subjects)` (`lib/meta.ts`), `timeAgo(date, lang)`, `relativeNoteDate(date, lang)` (todos en `lib/utils.ts` o `lib/meta.ts`)
- ✅ AI endpoint: `POST /api/ai` con `{ message, history, app_context: { current_page, active_subject_id?, language, subject_count?, pending_task_count?, next_exam_date? }, access_token }` → `{ reply, tools_used }`. JSON, no streaming.

---

## Estado git al cerrar esta sesión

```
$ git log -3 --oneline
7c168cb fix(notes): cuadrar shell — full-bleed bajo topbar, toolbar 1 fila, paddings tighter
151e056 feat(notes): rebuild visual v2 — 3-pane editorial, dual-style title, expanded toolbar
4bc1e61 fix(routing): + Nuevo y todos los CTAs a /tareas o /evaluaciones, no /planner
```

Branch `main`, todo pusheado. Vercel project `scholr` auto-desplegando.
Untracked: ver §"Limpieza opcional".

---

## Memoria del usuario relevante (no repetir)

- Email de prueba: `sebastiansequeirab@gmail.com` (11 notes, 3 tasks, 32 exams, 101 subjects).
- Idioma default: ES (`'es'` en `profiles.language`, cookie `skolar_lang`).
- Cada cambio termina con commit + push + Vercel auto-deploy (proyecto `scholr`).
- Lenguaje habitual: español.
- No hardcodear hex; usar CSS vars semánticas.
- Mantener i18n es/en en sync siempre.
- Sin `any` en TypeScript.
- `.env` files local-only — nunca pedir contenido en chat, leer con tools.
