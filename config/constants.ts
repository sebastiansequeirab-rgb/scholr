/**
 * Re-exports of app-wide constants.
 * Source of truth lives in types/index.ts to avoid circular imports.
 * New code can import from here for semantic clarity.
 */
export { ACTIVITY_TYPES, SUBJECT_COLORS, THEME_COLORS } from '@/types'
